# 평일 15:50 KST: 당일 정기공시를 스캔해 대상 기업만 증분 수집한다(daily_scheduler.py).
# cron 자체는 /proc/1/fd/1(컨테이너 stdout) 리다이렉션 권한 때문에 root로
# 기동한다(docker_guide.md §3).
50 15 * * 1-5 root /app/docker/run-daily.sh >> /proc/1/fd/1 2>&1
